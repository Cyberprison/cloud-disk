docker compose файл на будущее
для сервера на ASP dot Net Core Web API, реализую REST API сервер ну там ещё куча фич будет
для клиента на реакте свой докер файл
для БД монго свой докер файл
билд в докер помпоус, разверну на кубере миникуб
в будущем мб сервак арендую

как вот правильно сделать...
реакт, монго и асп в отдельных образах
и соединить сервисы в одном docker compose

понял
реакт и асп в образах, тк их собрать нада
а монго и так есть
вопрос только в том где данные бэкапить, как в реальном продакшене
позже займусь вопросом

#docker build -t "cloud-disk-server" .
#docker run --rm -d -p 5000:80 "cloud-disk-server" 

#docker build -t "cloud-disk-client" .
#docker run --rm -d -p 9000:9000 "cloud-disk-client"

github action ci/cd добавить
workflow etc

#docker-compose up -d 
#docker-compose down

скачать пакет для монго
придумать как чо работать из асп с монго
в еф, в монго пакете или через даппер

а вот как будут схемы бд сохраняться
наверное либо какие нибудь volume придумать
или миграции EF при каждом запуске применять
забыл вообще как локально работают БД

docker run --rm -d -p 27017:27017 --name my-mongodb -e MONGO_INITDB_ROOT_USERNAME=login -e MONGO_INITDB_ROOT_PASSWORD=pass mongo:latest